---
/**
 * /2 — Project Dossier (full HNI client dossier).
 * Served verbatim for the same reason as /1.
 */
import html from '../documents/project-dossier.html?raw';
---

<Fragment set:html={html} />
